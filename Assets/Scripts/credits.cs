using UnityEngine;

public class credits : MonoBehaviour {

    public void Quit()
    {
        Application.Quit(); // The game will in quit inside unity but when the game is exported it will quit
    }
}
